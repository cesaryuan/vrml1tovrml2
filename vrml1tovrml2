#!/usr/bin/env bash
# Forward to the Python implementation so Linux users get a simple CLI entrypoint.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "${SCRIPT_DIR}/vrml1tovrml2.py" "$@"
